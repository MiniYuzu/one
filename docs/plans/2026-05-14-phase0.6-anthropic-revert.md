# Phase 0.6 Anthropic 协议回退计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 彻底废弃客户端 OpenAI 兼容适配层，将引擎完全回退到原生 Anthropic `/v1/messages` 协议。未来对 OpenAI/MiniMax 等模型的兼容交由后端 AI 网关（如 LiteLLM）处理。

**Architecture:** 删除 `streamAdapter.ts` + `openaiClient.ts` 的翻译层，新建 `anthropicClient.ts` 直接消费 Anthropic 原生 SSE 事件（`StreamEvent` 1:1 映射）。重写 `query.ts` 的 `toApiMessages`，将内部 `Message[]` 严格映射为 Anthropic `MessageParam` 格式（保留 `tool_use`/`tool_result` block 结构，严禁 stringify 降级）。渲染层和 IPC 事件完全不受影响。

**Tech Stack:** Electron 28+ UtilityProcess, TypeScript, axios, eventsource-parser, React 18

---

## 文件结构总览

### 删除文件
- `src/engine/api/streamAdapter.ts` — OpenAI SSE → StreamEvent 适配器，彻底废弃
- `src/engine/api/openaiClient.ts` — OpenAI 兼容流式客户端，彻底废弃

### 新建文件
- `src/engine/api/anthropicClient.ts` — 原生 Anthropic `/v1/messages` 流式客户端

### 修改文件
- `src/engine/query/query.ts` — 重写 `toApiMessages`：Message[] → Anthropic MessageParam[]
- `src/engine/query/queryDeps.ts` — `callModel` 入参类型对齐 Anthropic 格式
- `src/engine/engine.ts` — 切换 `callModel` 绑定从 `streamChatCompletion` 到 `streamAnthropicMessages`

### 禁止修改的文件（约束条件）
- `src/renderer/App.tsx`
- `src/renderer/components/chat/MessageList.tsx`
- `src/renderer/components/chat/ToolCallCard.tsx`
- `src/shared/ipc-types.ts`（类型已兼容，无需扩展）

---

## Task 1: 清理历史包袱（Delete Adapters）

**Files:**
- Delete: `src/engine/api/streamAdapter.ts`
- Delete: `src/engine/api/openaiClient.ts`

**Context:** 这两个文件是 Phase 0.5 中 OpenAI 兼容层的核心。`streamAdapter.ts` 负责将 OpenAI 的 `choices[0].delta.content` 强行翻译成 `StreamEvent`；`openaiClient.ts` 负责通过 axios 请求 OpenAI-compatible 端点。由于架构决策彻底废弃客户端翻译层，这两个文件不再有任何价值。

- [ ] **Step 1: 删除 streamAdapter.ts**

```bash
rm src/engine/api/streamAdapter.ts
```

- [ ] **Step 2: 删除 openaiClient.ts**

```bash
rm src/engine/api/openaiClient.ts
```

- [ ] **Step 3: Commit**

```bash
git rm src/engine/api/streamAdapter.ts src/engine/api/openaiClient.ts
git commit -m "chore(engine): remove OpenAI adapter layer — streamAdapter.ts + openaiClient.ts"
```

---

## Task 2: 重塑 API Client 层（Create anthropicClient.ts）

**Files:**
- Create: `src/engine/api/anthropicClient.ts`

**Context:** 参考 `vendor/free-code/src/services/api/claude.ts` 的流式消费架构，但使用 axios + eventsource-parser（而非 `@anthropic-ai/sdk`）实现，以保持依赖精简。

核心设计原则：
1. **原生事件透传**：Anthropic SSE 的 `message_start` / `content_block_start` / `content_block_delta` / `content_block_stop` / `message_delta` / `message_stop` 直接映射为 `StreamEvent`，1:1 无转换。
2. **内联重试**：`withRetry.ts` 的 `execute` 签名是 `Promise<unknown>`，无法直接包裹 async generator。因此重试逻辑以内联 `while` 实现，但复用 `getRetryDelay()` 和 `is529Error()` 保证行为一致。
3. **Anthropic 请求头**：`Anthropic-Version: 2023-06-01`、`x-api-key: <apiKey>`。
4. **请求体**：`model`、`messages`（Anthropic `MessageParam[]`）、`max_tokens`、`stream: true`、`system`、`tools`（Anthropic 原生 schema：`{ name, description, input_schema }`）、`tool_choice: { type: 'auto' }`。

- [ ] **Step 1: 创建 anthropicClient.ts**

```typescript
// src/engine/api/anthropicClient.ts
import axios from 'axios'
import { createParser, type EventSourceMessage } from 'eventsource-parser'
import type { StreamEvent, SystemAPIErrorMessage, Usage } from '../types/message.js'
import type { Tool } from '../types/tool.js'
import { classifyAPIError } from './errors.js'
import { getRetryDelay, is529Error } from './withRetry.js'

// ---------------------------------------------------------------------------
// Anthropic 原生 SSE 事件结构（/v1/messages streaming）
// ---------------------------------------------------------------------------

export interface AnthropicSSEChunk {
  type: 'message_start' | 'content_block_start' | 'content_block_delta' | 'content_block_stop' | 'message_delta' | 'message_stop' | 'ping'
  message?: {
    id: string
    type: 'message'
    role: 'assistant'
    content: []
    model: string
    stop_reason?: string | null
    stop_sequence?: string | null
    usage?: { input_tokens: number; output_tokens: number }
  }
  index?: number
  content_block?:
    | { type: 'text'; text: string }
    | { type: 'tool_use'; id: string; name: string; input: Record<string, unknown> }
    | { type: 'thinking'; thinking: string; signature?: string }
  delta?:
    | { type: 'text_delta'; text: string }
    | { type: 'input_json_delta'; partial_json: string }
    | { type: 'thinking_delta'; thinking: string }
    | { type: 'signature_delta'; signature: string }
  usage?: { input_tokens: number; output_tokens: number }
}

export interface CallModelOptions {
  model: string
  baseUrl: string
  apiKey: string | null
  maxTokens?: number
  temperature?: number
  tools?: Tool[]
  signal: AbortSignal
  fallbackModel?: string
  onStreamingFallback?: () => void
}

// ---------------------------------------------------------------------------
// Anthropic SSE → StreamEvent（1:1 透传，无协议转换）
// ---------------------------------------------------------------------------

function* adaptAnthropicSSEToStreamEvents(chunk: AnthropicSSEChunk): Generator<StreamEvent> {
  switch (chunk.type) {
    case 'message_start': {
      yield {
        type: 'message_start',
        message: {
          usage: chunk.message?.usage
            ? {
                inputTokens: chunk.message.usage.input_tokens,
                outputTokens: chunk.message.usage.output_tokens,
              }
            : undefined,
        },
      }
      break
    }
    case 'content_block_start': {
      if (chunk.content_block) {
        const block = chunk.content_block
        // 【关键修复】：防御 Anthropic 原生的 input: {} 导致字符串拼接出现 [object Object]
        if (block.type === 'tool_use') {
          yield {
            type: 'content_block_start',
            index: chunk.index ?? 0,
            content_block: { ...block, input: '' } as any,
          }
        } else {
          yield {
            type: 'content_block_start',
            index: chunk.index ?? 0,
            content_block: block as any,
          }
        }
      }
      break
    }
    case 'content_block_delta': {
      if (chunk.delta) {
        yield {
          type: 'content_block_delta',
          index: chunk.index ?? 0,
          delta: chunk.delta as any,
        }
      }
      break
    }
    case 'content_block_stop': {
      yield { type: 'content_block_stop', index: chunk.index ?? 0 }
      break
    }
    case 'message_delta': {
      yield {
        type: 'message_delta',
        usage: chunk.usage
          ? {
              inputTokens: chunk.usage.input_tokens ?? 0,
              outputTokens: chunk.usage.output_tokens ?? 0,
            }
          : undefined,
        stop_reason: chunk.delta?.stop_reason ?? null,
      }
      break
    }
    case 'message_stop': {
      yield { type: 'message_stop' }
      break
    }
    case 'ping': {
      // Anthropic 心跳事件，无需透传
      break
    }
  }
}

// ---------------------------------------------------------------------------
// 流式请求入口
// ---------------------------------------------------------------------------

export async function* streamAnthropicMessages(
  messages: Array<{ role: string; content: unknown }>,
  systemPrompt: string | string[],
  options: CallModelOptions,
): AsyncGenerator<StreamEvent | SystemAPIErrorMessage, void> {
  const url = `${options.baseUrl.replace(/\/$/, '')}/v1/messages`
  const system = Array.isArray(systemPrompt) ? systemPrompt.join('\n\n') : systemPrompt

  const body: Record<string, unknown> = {
    model: options.model,
    messages,
    max_tokens: options.maxTokens ?? 4096,
    stream: true,
    ...(system && { system }),
    ...(options.temperature !== undefined && { temperature: options.temperature }),
    ...(options.tools && options.tools.length > 0 && {
      tools: options.tools.map(t => ({
        name: t.name,
        description: t.description,
        input_schema: t.inputSchema,
      })),
      tool_choice: { type: 'auto' },
    }),
  }

  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    Accept: 'text/event-stream',
    'Anthropic-Version': '2023-06-01',
  }
  if (options.apiKey) {
    headers['x-api-key'] = options.apiKey
  }

  let attempt = 0
  const maxRetries = 3

  while (attempt <= maxRetries) {
    try {
      if (options.signal.aborted) throw new Error('ABORTED')

      const response = await axios({
        method: 'post',
        url,
        data: body,
        headers,
        responseType: 'stream',
        signal: options.signal,
        timeout: 120000,
      })

      const eventQueue: StreamEvent[] = []
      const parser = createParser({
        onEvent: (event: EventSourceMessage) => {
          if (event.data === '[DONE]') return
          try {
            const json = JSON.parse(event.data) as AnthropicSSEChunk
            for (const evt of adaptAnthropicSSEToStreamEvents(json)) {
              eventQueue.push(evt)
            }
          } catch {
            // ignore malformed JSON in stream
          }
        },
      })

      const decoder = new TextDecoder()
      for await (const chunk of response.data) {
        if (options.signal.aborted) break
        const text = typeof chunk === 'string' ? chunk : decoder.decode(chunk, { stream: true })
        parser.feed(text)
        while (eventQueue.length > 0) {
          yield eventQueue.shift()!
        }
      }
      const flush = decoder.decode()
      if (flush) parser.feed(flush)
      while (eventQueue.length > 0) {
        yield eventQueue.shift()!
      }
      return
    } catch (error) {
      if (axios.isCancel(error) || options.signal.aborted) return
      attempt++
      const isAxiosErr = axios.isAxiosError(error)
      const status = isAxiosErr ? error.response?.status : undefined
      const isNetworkError = isAxiosErr && !error.response
      const isRetryable =
        status === 429 ||
        (status !== undefined && status >= 500) ||
        isNetworkError ||
        is529Error(error)

      if (!isRetryable || attempt > maxRetries) {
        const errorData =
          isAxiosErr && error.response?.data instanceof Buffer
            ? error.response.data.toString()
            : (error as Error).message
        yield {
          type: 'system',
          subtype: 'api_error',
          content: `请求被拒绝 (HTTP ${status ?? '网络'}): ${errorData}`,
          errorCode: 'API_ERROR',
        }
        return
      }

      yield {
        type: 'system',
        subtype: 'api_error',
        content: `网络繁忙，正在重试 (${attempt}/${maxRetries})...`,
        errorCode: 'RETRY',
      }
      await new Promise(r => setTimeout(r, getRetryDelay(attempt)))
    }
  }
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 零错误（streamAdapter.ts 和 openaiClient.ts 已删除，无残留 import）

- [ ] **Step 3: Commit**

```bash
git add src/engine/api/anthropicClient.ts
git commit -m "feat(engine): add native Anthropic /v1/messages streaming client"
```

---

## Task 3: 修复上行数据污染（Revert query.ts）

**Files:**
- Modify: `src/engine/query/query.ts`

**Context:** 当前 `toApiMessages` 将 `Message[]` 降级为 OpenAI-compatible 格式（`{ role, content: string | null, tool_calls }`）。这导致 Anthropic 原生的 `tool_use`/`tool_result` block 结构被 stringify 为纯文本，严重破坏状态机。

修复目标：将内部 `Message[]` 严格映射为 Anthropic `MessageParam` 格式：
- **UserMessage** → `{ role: 'user', content: [{ type: 'text', text }, { type: 'tool_result', tool_use_id, content, is_error }] }`
- **AssistantMessage** → `{ role: 'assistant', content: [{ type: 'text', text }, { type: 'tool_use', id, name, input }] }`
- **SystemMessage** 不在 `messages` 数组中传递，而是通过 `callModel` 的 `systemPrompt` 参数单独注入。

- [ ] **Step 1: 重写 toApiMessages**

```typescript
// src/engine/query/query.ts — 替换整个 toApiMessages 函数
function toApiMessages(messages: Message[]): Array<{ role: 'user' | 'assistant'; content: unknown }> {
  return messages
    .filter((m): m is UserMessage | AssistantMessage => m.type === 'user' || m.type === 'assistant')
    .map((m) => {
      if (m.type === 'user') {
        const blocks = m.content.map((c) => {
          if (c.type === 'text') {
            return { type: 'text', text: c.text }
          }
          if (c.type === 'tool_result') {
            return {
              type: 'tool_result',
              tool_use_id: c.tool_use_id,
              content: typeof c.content === 'string' ? c.content : JSON.stringify(c.content),
              is_error: c.is_error,
            }
          }
          return c
        })
        return { role: 'user', content: blocks }
      }

      // assistant
      const blocks = m.content.map((c) => {
        if (c.type === 'text') {
          return { type: 'text', text: c.text }
        }
        if (c.type === 'tool_use') {
          return {
            type: 'tool_use',
            id: c.id,
            name: c.name,
            input: typeof c.input === 'string' ? JSON.parse(c.input || '{}') : c.input,
          }
        }
        return c
      })
      return { role: 'assistant', content: blocks }
    })
}
```

- [ ] **Step 2: 验证类型一致性**

确认 `query.ts` 中以下类型引用无需修改：
- `StreamEvent` 消费逻辑（`message_start` / `content_block_start` / `content_block_delta` / `content_block_stop` / `message_delta` / `message_stop`）—— 这些事件名本就是 Anthropic 原生格式，直接透传无需改动。
- `ContentBlock` 类型中 `tool_use`（`id`, `name`, `input`）和 `tool_result`（`tool_use_id`, `content`, `is_error`）与 Anthropic block 结构完全对齐。

Run: `npx tsc --noEmit`
Expected: 零错误

- [ ] **Step 3: Commit**

```bash
git add src/engine/query/query.ts
git commit -m "fix(engine): map Message[] to native Anthropic MessageParam format in toApiMessages"
```

---

## Task 4: 对齐依赖注入（Update queryDeps.ts & engine.ts）

**Files:**
- Modify: `src/engine/query/queryDeps.ts`
- Modify: `src/engine/engine.ts`

**Context:** `queryDeps.ts` 中 `callModel` 的 `messages` 参数类型仍是 OpenAI-compatible 的 `{ role: string; content?: string | null | unknown[]; tool_calls?: unknown[]; tool_call_id?: string }[]`。必须改为 Anthropic `MessageParam` 的简化表示：`Array<{ role: 'user' | 'assistant'; content: unknown }>`。

`engine.ts` 中 `createQueryEngine` 的 `callModel` 绑定必须从 `streamChatCompletion`（已删除）切换到 `streamAnthropicMessages`。

- [ ] **Step 1: 修改 queryDeps.ts**

```typescript
// src/engine/query/queryDeps.ts
import type { StreamEvent, SystemAPIErrorMessage } from '../types/message.js'
import type { Tool } from '../types/tool.js'

export interface QueryDeps {
  callModel: (
    messages: Array<{ role: 'user' | 'assistant'; content: unknown }>,
    systemPrompt: string[],
    options: {
      model: string
      baseUrl: string
      apiKey: string | null
      signal: AbortSignal
      tools?: Tool[]
    },
  ) => AsyncGenerator<StreamEvent | SystemAPIErrorMessage, void>
  uuid: () => string
}
```

- [ ] **Step 2: 修改 engine.ts**

```typescript
// src/engine/engine.ts — 修改 import 和 createQueryEngine 中的绑定

// 删除以下 import：
// import { streamChatCompletion } from './api/openaiClient.js'

// 新增 import：
import { streamAnthropicMessages } from './api/anthropicClient.js'

// 在 createQueryEngine 函数中，将 callModel 绑定替换为：
// BEFORE:
//   callModel: (messages, systemPrompt, options) =>
//     streamChatCompletion(messages, systemPrompt, { ... }),

// AFTER:
//   callModel: (messages, systemPrompt, options) =>
//     streamAnthropicMessages(messages, systemPrompt, { ... }),
```

具体 diff：

```typescript
// src/engine/engine.ts 第 100-107 行附近
      callModel: (messages, systemPrompt, options) =>
        streamAnthropicMessages(messages, systemPrompt, {
          model: options.model,
          baseUrl: options.baseUrl,
          apiKey: options.apiKey,
          signal: options.signal,
          tools: options.tools,
        }),
```

- [ ] **Step 3: 验证编译与端到端链路**

Run: `npx tsc --noEmit`
Expected: 零错误

- [ ] **Step 4: Commit**

```bash
git add src/engine/query/queryDeps.ts src/engine/engine.ts
git commit -m "refactor(engine): align callModel deps with Anthropic native protocol"
```

---

## 自检

### 1. Spec Coverage

| 审查要求 | 实现 Task | 状态 |
|---|---|---|
| 彻底删除 OpenAI 适配层（streamAdapter + openaiClient） | Task 1 | `git rm` 两文件 |
| 新建原生 Anthropic 流式客户端（/v1/messages） | Task 2 | `anthropicClient.ts` 直接消费原生 SSE |
| 保留重试机制（529/429/网络错误） | Task 2 | 内联循环复用 `getRetryDelay` + `is529Error` |
| 修复 Message[] → Anthropic MessageParam 映射 | Task 3 | `toApiMessages` 重写，禁止 stringify |
| 对齐 queryDeps.ts 类型签名 | Task 4 | `messages` 参数改为 Anthropic 格式 |
| 切换 engine.ts 模型驱动 | Task 4 | `streamAnthropicMessages` 替换 `streamChatCompletion` |
| 不修改渲染层 | 全局约束 | App.tsx / MessageList.tsx / ToolCallCard.tsx 零变更 |
| 保留 workingDirectory 沙盒 | 全局约束 | `query.ts` 中 `runTools` 的 `workingDirectory` 参数未改动 |

### 2. Placeholder Scan

- [x] 无 "TBD"、"TODO"、"implement later"
- [x] 无 "Add appropriate error handling" 等模糊描述
- [x] 每个代码步骤包含可直接运行的 TypeScript
- [x] 无 "Similar to Task N" 跨任务引用

### 3. Type Consistency

- `StreamEvent` 在 `message.ts` 中定义（Task 未修改），Task 2 的 `adaptAnthropicSSEToStreamEvents` 直接产出该类型。
- `ContentBlock` 中 `tool_use.id` / `tool_use.name` / `tool_use.input` 与 Anthropic API 的 `ToolUseBlock` 结构一致。
- `ContentBlock` 中 `tool_result.tool_use_id` / `tool_result.content` / `tool_result.is_error` 与 Anthropic API 的 `ToolResultBlock` 结构一致。
- `queryDeps.ts` 中 `messages` 参数类型 `Array<{ role: 'user' | 'assistant'; content: unknown }>` 与 Anthropic `MessageParam` 的子集等价。

---

## 执行选项

**Plan complete and saved to `docs/plans/2026-05-14-phase0.6-anthropic-revert.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration. Each task produces working, testable code on its own.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints for review.

**Which approach?**
